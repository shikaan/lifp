{
  "name": "lifp",
  "main": "bin/index.ts",
  "scripts": {
    "start": "bun --define __VERSION__=\"'v0.0.0 (dev)'\" .",
    "build": "bun build . --compile --outfile build/lifp",
    "format": "bunx --bun biome format --write",
    "lint": "bunx --bun biome format",
    "postinstall": "mkdir -p .git/hooks && cp ./scripts/pre-commit .git/hooks/pre-commit && chmod +x .git/hooks/pre-commit"
  },
  "engines": {
    "bun": "1.2.13"
  },
  "author": "Manuel Spagnolo",
  "license": "MIT",
  "type": "module",
  "devDependencies": {
    "@biomejs/biome": "2.0.2",
    "@types/node": "^22.15.31",
    "@types/bun": "latest"
  },
  "private": true,
  "peerDependencies": {
    "typescript": "^5"
  }
}
